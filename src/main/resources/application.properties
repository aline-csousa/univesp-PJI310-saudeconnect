# application.properties
# Conexão com o MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/saude_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=senha123  # Substitua pela sua senha real
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Porta do servidor (opcional)
server.port=8080

# Configurações adicionais
spring.jpa.open-in-view=false

# Configuração Z-API
zapi.url=https://api.z-api.io/instances/3E0DDA8E2791104AD11C3EF0F02679BC
zapi.token=2D814E9CB8123531F72992BF